[project]
name = "fp-smart-connect"
version = "0.1.0"
description = "Fisher-Price Smart Connect integration for Home Assistant"
readme = "README.md"
requires-python = ">=3.14.7"
dependencies = []

[dependency-groups]
dev = ["ruff>=0.16.6"]
